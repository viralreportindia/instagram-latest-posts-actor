{
  "title": "Instagram Latest Posts - Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "usernames": {
      "title": "Instagram Usernames",
      "type": "array",
      "description": "List of Instagram usernames to scrape (without @)",
      "editor": "stringList",
      "example": ["iprettyjangra", "twobhkhe", "sunnyleone"]
    },
    "count": {
      "title": "Number of posts per user",
      "type": "integer",
      "description": "How many latest video posts to fetch per username (max 12)",
      "default": 3,
      "minimum": 1,
      "maximum": 12,
      "editor": "number"
    },
    "includeImages": {
      "title": "Include image posts too?",
      "type": "boolean",
      "description": "If false, only video/reels are returned",
      "default": false,
      "editor": "checkbox"
    }
  },
  "required": ["usernames"]
}
